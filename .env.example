# Server
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/task-7

# Session / JWT
JWT_SECRET=replace-with-strong-secret

# OAuth (update callback URLs in Render dashboard if using custom domain)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:3000/auth/github/callback

# APIs
WEATHER_API_KEY=
NEWS_API_KEY=

# Optional: Override base URL used to compute callbacks (else uses RENDER_EXTERNAL_URL)
# BASE_URL=https://your-service.onrender.com